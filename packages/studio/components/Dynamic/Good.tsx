import { Box, Button } from "@mui/material";

const WER = () => {
  return (
    <Box sx={{ width: "100%", height: "100%", background: "orange" }}>
      <Button>good</Button>
    </Box>
  );
};

export default WER;
