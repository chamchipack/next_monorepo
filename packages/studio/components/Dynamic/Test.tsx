import { Box, Button } from "@mui/material";

const Test = () => {
  return (
    <Box sx={{ width: "100%", height: "100%", background: "skyblue" }}>
      <Button>후후후후</Button>
    </Box>
  );
};

export default Test;
